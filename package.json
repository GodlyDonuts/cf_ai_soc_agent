{
  "name": "cf_ai_soc_agent",
  "private": true,
  "workspaces": [
    "soc-agent",
    "frontend",
    "enforcement-worker"
  ],
  "scripts": {
    "test": "npm run test -w soc-agent && npm run test -w frontend",
    "lint": "npm run lint -w frontend",
    "build": "npm run build -w soc-agent && npm run build -w frontend"
  }
}
